/* 
Tuple is a typescript type that works like an array with some special considerations:
The number of elements of the array are fixed.
The type of the elements of the array need not be the same.
They are useful when we have 2 key value pairs.s
*/
let user: [number, string] = [1, "Peter"];
